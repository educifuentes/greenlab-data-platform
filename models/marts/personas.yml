semantic_models:
  - name: personas
    defaults:
      agg_time_dimension: fecha_nacimiento
    description: "tabla anivel de personas 100k filas sample"
    entities:
      - name: persona_id
        type: primary
      - name: hogar_id
        type: foreign
      - name: vivienda_id
        type: foreign
    dimensions:
      - name: gender
        type: categorical
    measures:
      - name: count
        type: count


hacer porcentaje por p31_religion


p31_religion